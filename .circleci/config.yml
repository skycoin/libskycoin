version: 2
jobs:
  build:
    docker: # use the docker executor type; machine and macos executors are also supported
      - image: balenalib/raspberrypi3-ubuntu-golang # the primary container, where your job's commands are run
    steps:
      - run: [ "cross-build-start" ]
      - run: date