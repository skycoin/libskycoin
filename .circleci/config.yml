version: 2
jobs:
  build:
    working_directory: $GOPATH/src/github.com/skycoin/libskycoin
    docker: # use the docker executor type; machine and macos executors are also supported
      - image: circleci/golang # the primary container, where your job's commands are run

    environment:
      BUILD_DIR: build
      BUILDLIB_DIR: $BUILD_DIR/libskycoin
      LIB_DIR: lib
      CGO_ENABLED: 1

    steps:
      - run: mkdir -p $GOPATH/src/github.com/ $GOPATH/src/github.com/skycoin
      - checkout # check out the code in the project directory
      - run: mkdir -p $GOPATH/src/github.com/skycoin
      - run: cd $GOPATH/src/github.com/skycoin && git clone --branch v0.25.1 https://github.com/skycoin/skycoin 
      - run: ls -os $CIRCLE_WORKING_DIRECTORY
      # - run: make -C $CIRCLE_WORKING_DIRECTORY dep
      # - run: $CIRCLE_WORKING_DIRECTORY/ci-scripts/install-travis-gcc.sh
      # - run: go get github.com/gz-c/gox
      # - run: go get -t ./...
      # - run: make install-linters
      # - run: VERSION=1.10.2 ./ci-scripts/install-golangci-lint.sh
      # - run: make -C $CIRCLE_WORKING_DIRECTORY install-deps-libc-linux
      # - run: make -C $CIRCLE_WORKING_DIRECTORY install-lib-curl
      # - run: make -C $CIRCLE_WORKING_DIRECTORY check
